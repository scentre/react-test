import { Text } from "@chakra-ui/react";

const Home = () => {
  return (
    <Text fontFamily={"circular-std"} fontWeight={"normal"} fontSize={28}>
      ProfileContent
    </Text>
  );
};

export default Home;
